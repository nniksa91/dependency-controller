apiVersion: apps/v1
kind: Deployment
metadata:
  name: dependency-controller
  namespace: job-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dependency-controller
  template:
    metadata:
      labels:
        app: dependency-controller
    spec:
      serviceAccountName: dependency-controller-sa
      containers:
        - name: manager
          image: dependency-controller:jobdeployment
          command:
            - /manager
          ports:
            - containerPort: 8080
              name: metrics
              protocol: TCP
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
            requests:
              cpu: 100m
              memory: 64Mi
