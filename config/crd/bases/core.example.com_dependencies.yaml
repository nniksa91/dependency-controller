---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: dependencies.core.example.com
spec:
  group: core.example.com
  names:
    kind: Dependency
    listKind: DependencyList
    plural: dependencies
    singular: dependency
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Dependency is the Schema for the dependencies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DependencySpec defines the desired state of Dependency
            properties:
              dependency:
                description: Dependency is the name of the deployment that the Dependent
                  deployment relies on
                type: string
              dependent:
                description: Dependent is the name of the deployment that depends
                  on the Dependency deployment
                type: string
            required:
            - dependency
            - dependent
            type: object
          status:
            description: DependencyStatus defines the observed state of Dependency
            properties:
              dependencyReady:
                description: DependencyReady indicates if the Dependency deployment
                  is fully ready
                type: boolean
              dependentScaledDown:
                description: DependentScaledDown indicates if the Dependent deployment
                  is scaled down
                type: boolean
            required:
            - dependencyReady
            - dependentScaledDown
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
