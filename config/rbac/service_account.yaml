apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: dependency
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager
  namespace: system
# Controller must call the API server; never set false on this SA.
automountServiceAccountToken: true
