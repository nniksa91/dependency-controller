apiVersion: core.example.com/v1
kind: Dependency
metadata:
  name: my-dependency2
  namespace: job-deployment
spec:
  dependency: pod1-deployment
  dependencyType: Deployment
  dependent: pod2-deployment
  dependentType: Deployment
